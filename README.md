# Handwritten-digit-recognition
A simple CNN model trained on the MNSIT handwritten digits dataset. The model is trained in a Jupyter Notebook and is then consumed in a simple Flask Backed. I've made a small canvas element in the Angular frontend to drawn your own digits and get a predicton back.
